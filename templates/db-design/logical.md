# {{프로젝트명}} 논리적 설계 (Logical Design)

> 선행: `conceptual.md` (ERD)
> 파이프라인 6단계 산출물

---

## 정규화 결과

### {{엔티티1}}
| 속성 | 타입 | NULL | 제약 | 정규화 근거 |
|------|------|------|------|------------|
| id | BIGINT | NO | PK, AUTO_INCREMENT | 대리키 |
| {{속성}} | {{타입}} | {{YES/NO}} | {{UK/FK/CHECK}} | {{정규화 단계}} |

### {{엔티티2}}
| 속성 | 타입 | NULL | 제약 | 정규화 근거 |
|------|------|------|------|------------|
| id | BIGINT | NO | PK, AUTO_INCREMENT | 대리키 |
| {{FK명}} | BIGINT | NO | FK → {{참조 테이블}} | {{관계}} |

---

## 정규화 검증

| 검증 항목 | 결과 | 비고 |
|----------|------|------|
| 1NF (원자성) | {{PASS/FAIL}} | {{반복 그룹 여부}} |
| 2NF (부분 종속) | {{PASS/FAIL}} | {{복합키 부분 종속 여부}} |
| 3NF (이행 종속) | {{PASS/FAIL}} | {{이행적 종속 여부}} |

---

## 반정규화 결정

| 대상 | 반정규화 기법 | 사유 | 트레이드오프 |
|------|-------------|------|------------|
| {{대상 (없으면 "해당 없음")}} | {{파생 컬럼/테이블 병합/JSON}} | {{읽기 비율/집계 빈도}} | {{갱신 이상 위험}} |

---

## PK/FK 전략

### PK
- **전략**: {{대리키 BIGINT AUTO_INCREMENT / 자연키}}
- **UUID**: {{사용 여부 + 사유}}

### FK 참조 무결성
| FK | 참조 | ON DELETE | 사유 |
|----|------|-----------|------|
| {{테이블}}.{{컬럼}} | {{참조 테이블}} | {{RESTRICT/CASCADE}} | {{사유}} |

### Aggregate 경계 (DDD 적용 시)
| Aggregate Root | 내부 엔티티 | 외부 참조 (ID만) |
|---------------|-----------|----------------|
| {{Root}} | {{자식들}} | {{외부 Aggregate ID}} |

---

## 인덱스 후보

| 테이블 | 인덱스 | 컬럼 | 사유 |
|--------|--------|------|------|
| {{테이블}} | idx_{{테이블}}_{{용도}} | {{컬럼들}} | {{WHERE/JOIN/ORDER BY}} |

---

## 상태 전이 ↔ enum 매핑

| 도메인 | enum 컬럼 | 값 목록 | PRD 상태 전이 참조 |
|--------|----------|---------|------------------|
| {{도메인}} | status | {{값들}} | {{도메인}}.md 상태 전이 규칙 |

<!-- 검증 기준:
- 모든 ERD 엔티티가 논리 설계에 반영
- 3NF 검증 PASS (또는 반정규화 사유 명시)
- PRD 상태 전이 규칙의 모든 상태값이 enum에 포함
- FK 참조 무결성 전략이 비즈니스 규칙과 일치
-->
