# 아키텍처 & 인프라 설계

---

## 1. 이벤트 아키텍처

### 이벤트 목록

| 이벤트 | Producer | Consumer | 토픽 | 비고 |
|--------|----------|----------|------|------|
| {{이벤트명 (과거형)}} | {{Service}} | {{Service}} | `{{토픽명}}` | {{설명}} |

### 동기 vs 비동기 경계

| 흐름 | 방식 | 사유 |
|------|------|------|
| {{흐름1}} | **동기** (같은 트랜잭션) | {{사유: 정합성 필수 등}} |
| {{흐름2}} | **비동기** (이벤트) | {{사유: 서비스 분리 등}} |

### Consumer 멱등성 전략

| Consumer | 멱등성 방법 |
|----------|-----------|
| {{Service}} | {{방법: eventId 중복 체크 / 상태 기반 무시 등}} |

### Consumer Group 설계
- 각 서비스는 **독립 Consumer Group**으로 운영
- 동일 토픽의 이벤트를 여러 서비스가 수신할 때 Fan-out

### 소비 실패 정책
- 처리 실패 시: **{{N}}회 재시도** ({{간격}})
- {{N}}회 실패 시: **Dead Letter Topic** (`{topic}.DLT`)
- DLT 메시지: 관리자 API로 조회/재처리

### 이벤트 페이로드 스키마

| 이벤트 | 페이로드 필드 |
|--------|-------------|
| {{이벤트명}} | `{ eventId, {{필드들}}, timestamp }` |

### 토픽 설정

| 토픽 | 파티션 | Replica | 키 | 이벤트 |
|------|--------|---------|-----|--------|
| {{토픽명}} | {{N}} | {{N}} | {{파티션 키}} | {{이벤트 목록}} |

---

## 2. 캐시 전략

| 캐시 대상 | 키 패턴 | TTL | 무효화 전략 |
|----------|---------|-----|------------|
| {{대상}} | `{{domain}}:{{identifier}}` | {{시간}} | {{전략: DELETE/TTL 자연 만료}} |

### 키 네이밍 규칙
- 콜론(`:`)으로 계층 구분: `domain:identifier:sub`

---

## 3. 제약조건

### 기술 제약
- {{기술 제약 1}}
- {{기술 제약 2}}

### 설계 제약
- **높은 응집도**: {{규칙}}
- **낮은 결합도**: {{규칙 + 동기 호출 허용 예외}}
- {{추가 설계 제약}}

### 환경 제약
- OS: {{운영 환경}}
- 컨테이너: {{컨테이너 환경}}
- 포트: {{서비스별 포트 매핑}}

---

## 4. API 역할 매트릭스

| 엔드포인트 패턴 | 공개 | {{역할1}} | {{역할2}} | {{역할3}} |
|----------------|------|----------|----------|----------|
| `{{METHOD}} {{path}}` | {{✓/-}} | {{✓/-}} | {{✓/-}} | {{✓/-}} |

- **공개**: 인증 없이 접근 가능
- **✓**: 해당 역할로 인증 시 접근 가능
- **-**: 접근 불가 (403 Forbidden)

---

<!-- 작성 체크리스트 (작성 완료 후 삭제) -->
<!--
- [ ] 모든 domain/*.md의 이벤트가 이벤트 목록에 반영
- [ ] 모든 이벤트에 페이로드 스키마 정의
- [ ] 동기/비동기 경계가 domain/*.md의 기술과 일치
- [ ] 모든 domain/*.md의 API가 역할 매트릭스에 반영
- [ ] notification.md의 알림 매핑과 이벤트 목록 일치 (해당 시)
-->
